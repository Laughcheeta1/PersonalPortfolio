{
    "name": "PersonalPortfolio",
    "targets": {
        "dev": {
            "executor": "nx:run-commands",
            "options": {
                "parallel": true,
                "commands": [
                    { "command": "nx dev frontend" },
                    { "command": "nx dev lambdas" }
                ]
            }
        }
    }
}